import React from "react";

const HolidaySettings = () => {
  return (
    <div>
      <h4 className="mb-4">Holiday Settings</h4>
      <p className="text-muted">Manage clinic holidays here.</p>
      {/* Content will go here */}
    </div>
  );
};

export default HolidaySettings;
