import React from "react";

const ListingSettings = () => {
  return (
    <div>
      <h4 className="mb-4">Listing Settings</h4>
      <p className="text-muted">Manage listings here.</p>
      {/* Content will go here */}
    </div>
  );
};

export default ListingSettings;
